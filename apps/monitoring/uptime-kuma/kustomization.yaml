apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: uptime-kuma

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  # Uncomment when you configure ingress:
  # - ingress.yaml

commonLabels:
  app: uptime-kuma
  managed-by: kustomize
